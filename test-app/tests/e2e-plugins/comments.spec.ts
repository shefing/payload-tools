import { test, expect } from '@playwright/test'
import { login } from './helpers'

test.describe.skip('CommentsPlugin (@shefing/comments)', () => {
/*  test.todo('comments panel is visible on the article edit page')
  test.todo('can add a new comment to a document')
  test.todo('comment is persisted and visible after page reload')
  test.todo('can delete an existing comment')*/
})
